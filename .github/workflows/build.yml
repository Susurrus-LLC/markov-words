name: CI

on: [push, pull_request]

jobs:
  install:
    name: Install
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: ./

      - name: Install
        run: yarn

  test:
    name: Test
    needs: install
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: ./
        run: yarn test

  deploy:
    name: Build and Deploy
    needs: test
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - name: Build and Deploy
        uses: ./
        run: yarn deploy
